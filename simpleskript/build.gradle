plugins {
	id 'application'
	id 'com.github.johnrengelman.shadow' version '5.2.0'
}

mainClassName = 'io.github.bensku.skripty.simple.Runner'

dependencies {
	compile project(':skripty-parser')
	compile project(':skripty-runtime')
}